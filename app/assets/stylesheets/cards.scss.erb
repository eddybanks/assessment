.report-type-card {
  min-height: 30%;
    text-align: center;
  @include links($jet, lighten($jet, 40%));
  .actions {
    text-align: center;
  }
}

.home-card-group {
  @include boxes(30vh, 1% 0, 1%, rgba($white, 0.4));
  @include fonts($open-sans-con, $jet);
}
