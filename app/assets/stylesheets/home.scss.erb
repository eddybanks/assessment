#home-jumbo-box {
    @include boxes(30vh, 0, 0, $af_white $home-big-pic);
}

.box-msg {
    @include boxes(30vh, 0, 6%, rgba($jet, 0.4));
    @include fonts($poiret-one, $af_white);
}

section {
    h2 {
        @include fonts($pt-sans, rgba($jet, .8), 2em);
    }
}
