#home-jumbo-box {
  @include boxes(25vh, 0, 0, $af_white $home-big-pic);
  @include links();
}

.box-msg {
  @include boxes(25vh, 0, 4%, rgba($jet, 0.4));
  @include fonts($open-sans, $af_white);
}

section {
  h2 {
    @include fonts($pt-sans, rgba($jet, .8), 2em);
  }
}

.assessment-type-info {
  min-height: 70vh;
}
