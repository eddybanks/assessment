nav #top-nav {
  background-color: $unm-color;
  margin: 0;
  padding: 0;
  border-radius: 0;
  .navbar-brand {
    // background-image: $unm-brand;
    margin: 0;
    padding: 0;
  }
}

header {
  .page-header {
    border-bottom: 3px solid rgba($jet, 0.3);
    margin-bottom: 2%;
  }
}

footer {
  color: white;
  background: $unm-color $footer-bg bottom right no-repeat;
  margin: 0;
  padding: 2% 0;
  @include links;
  @include fonts($dosis, $white, 0.8em);
}
