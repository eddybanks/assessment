<h1>Editing Template</h1>

<%= render 'form', template: @template %>

<%= link_to 'Show', @template %> |
<%= link_to 'Back', year_assessment_type_templates_path(current_year, @template.assessment_type) %>
