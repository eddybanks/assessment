<% if td.content.present? %>
  <h4><%= td.item.name %></h4>
  <p><%= simple_format td.content %></p>
<% else %>
  <div class="form-field">
    <%= t.input :content, label: td.item.name %>
  </div>
<% end %>
