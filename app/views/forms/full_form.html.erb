<div id="app">
  <div class="form-item">
    <select v-model='input-type'>
      <% InputType.all.each do |input_type| %>
        <option><%= input_type.name %></option>
      <% end %>
    </select>
    <div class="" v-if="input-type === 'text_field'">
      <p>
        Come on!
      </p>
    </div>
  </div>
  <button></button>
</div>
